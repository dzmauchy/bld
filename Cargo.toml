[workspace]
resolver = "2"
members = ["core", "ui"]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-only"

[workspace.dependencies]
leptos = { version = "0.8", features = ["csr"] }
bld_core = { path = "core", package = "core" }
