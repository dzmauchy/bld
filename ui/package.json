{
  "name": "ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-only",
  "scripts": {
    "dev": "rsbuild",
    "build": "tsc --noEmit && rsbuild build",
    "preview": "rsbuild preview"
  },
  "dependencies": {
    "core": "*"
  },
  "devDependencies": {
    "@rsbuild/core": "2.2.4"
  }
}
