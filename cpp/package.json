{
  "name": "cpp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-only",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./*": "./src/*"
  },
  "dependencies": {
    "modern-tar": "^0.8.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@rsbuild/core": "^2.2.5",
    "base": "*",
    "core": "*"
  },
  "scripts": {
    "sync-assets": "node scripts/sync-assets.mjs",
    "dev": "rsbuild",
    "build": "tsc -b && tsc -b tsconfig.test.json",
    "preview": "rsbuild build && rsbuild preview --port 3002",
    "test": "tsc -b && tsc -b tsconfig.test.json && vitest run tests",
    "test:e2e": "tsc -b tsconfig.test.json && playwright test",
    "clean": "tsc -b --clean"
  }
}
